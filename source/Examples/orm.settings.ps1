@{
    # Optional default database path used by your scripts
    DbPath   = 'C:\\path\\to\\app.db'

    # Log file path (omit to log via Write-Verbose)
    LogPath  = 'C:\\path\\to\\db.log'

    # Logging level: DEBUG | INFO | WARN | ERROR
    LogLevel = 'INFO'
}

